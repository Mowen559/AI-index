const fs = require('fs'); const path = require('path'); function walk(dir) { let hasSymlink = false; const entries = fs.readdirSync(dir, {withFileTypes:true}); for(const entry of entries) { if(entry.isSymbolicLink()) { console.log('Symlink found:', path.join(dir, entry.name)); hasSymlink = true; } else if(entry.isDirectory()) { hasSymlink = walk(path.join(dir, entry.name)) || hasSymlink; } } return hasSymlink; } console.log('Has symlinks?', walk('hub-web/.next/standalone/node_modules/.pnpm'));
